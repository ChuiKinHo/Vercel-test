const defaultStyle = "animate-spin rounded-full border-2 border-t-blue-600 border-r-transparent border-b-transparent border-l-transparent";

const styles = {
  default: `${defaultStyle} h-4 w-4`,
  small: `${defaultStyle} h-4 w-4`,
  medium: `${defaultStyle} h-10 w-10`,
  large: `${defaultStyle} h-16 w-16`,
};

export default styles;
