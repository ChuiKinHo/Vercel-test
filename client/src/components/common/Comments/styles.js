const defaultStyle = "flex flex-row gap-x-3 items-center pl-3 pr-5 py-1.5 bg-transparent hover:bg-s2/30 shadow-sm rounded-2xl";

const styles = {
  default: `${defaultStyle}`,
  "toilet-comment": "flex flex-row gap-x-3 items-center py-3 px-3 hover:bg-s2/20 rounded-lg"
};

export { styles };
