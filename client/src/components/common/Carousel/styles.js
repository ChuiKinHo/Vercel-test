const defaultStyle = "w-full h-full";

const styles = {
  default: `${defaultStyle}`,
  "gallery-page-carousel": `${defaultStyle}`,
  "add-toilet-drawer-carousel": `${defaultStyle}`,
};

export { styles };
