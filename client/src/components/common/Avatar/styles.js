const defaultStyle = `rounded-full object-cover`;

export const styles = {
  default: defaultStyle,
  "profile-page-friend-avatar": `${defaultStyle} border-4 border-blue-light p-1 shadow-md absolute top-1/2 left-0 -translate-x-1/2 -translate-y-1/2 `,
};
